<h1 align="center"> MiniMergeUI</h1>
<h2>📌 Описание сцены</h2>
● Canvas 3×3.
<br>● Каждая клетка — Image.
<br>● В каждой клетке может лежать одна фишка (иконка или цветной квадрат).
<h2>🎯 Перетаскивание (Drag & Drop)</h2>
Нужно реализовать полностью своё drag-and-drop решение.
<h3>Запрещено:</h3>
● UnityEngine.InputSystem
<br>● интерфейсы (IDragHandler, IDropHandler, etc.)
<br>● любые готовые drag & drop ассеты
<h3>Разрешено:</h3>
● старый input (mouse/touch)
<br>● DOTween по желанию
<h3>Поведение фишки:</h3>
при захвате — следует за курсором,

<h4>при отпускании:</h4>

если ячейка пустая → занимает её,

<br>если там фишка того же типа → merge,
<br>если чужая фишка → возвращается назад.
<h2>⚡ Merge-механика</h2>
Если две фишки одного типа объединяются:
<br>создаётся фишка следующего уровня,
<br>обе старые исчезают.
<h3>Обязательная анимация:</h3>
Scale → Scale * n, (n > 1) → Scale назад
<h3>Опционально:</h3>
● glow
<br>● fade
<br>● shake
<h2>➕ Спавн фишек</h2>
Кнопка Spawn добавляет новую фишку в любую пустую клетку.
<br>Если пустых клеток нет — ничего не происходит (можно показать сообщение).
<h2>🛑 Ограничения</h2>
● Только старый Input.
<br>● Никаких готовых drag-and-drop систем.
<br>● Логика целиком реализована вручную.
<br>● Любые визуальные ассеты допустимы.

